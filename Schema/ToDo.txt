
-- data fix
-- kardex fix

-- invert gender
UPDATE employee SET gender = 2 WHERE gender = 0;
UPDATE employee SET gender = 0 WHERE gender = 1;
UPDATE employee SET gender = 1 WHERE gender = 2;

SET sql_mode = 'NO_AUTO_VALUE_ON_ZERO';

INSERT INTO price_list
  (price_list_id, name, low_profit_margin, high_profit_margin)
VALUES
  (0, 'Costo', 0, 0);

INSERT INTO product_price
	(list, product, price, currency)
SELECT
	0, product_id, cost, 0
FROM product;

UPDATE customer SET code = CONCAT('C', RIGHT(CONCAT('0000', customer_id), 4));
