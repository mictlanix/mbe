@using Business.Essentials.Model
@model IEnumerable<Business.Essentials.Model.PointOfSale>

@{
    ViewBag.Title = Business.Essentials.Resources.Title_PointOfSale;
}

<h2>@Business.Essentials.Resources.DisplayName_PointsOfSale</h2>

<p>
    @Html.ActionLink(Business.Essentials.Resources.CreateNew, "Create")
</p>
<table>
    <tr>
        <th>
            @Html.LabelFor(x => new PointOfSale().Code)
        </th>

        <th>
            @Html.LabelFor(x => new PointOfSale().Name)
        </th>
        <th>
            @Html.LabelFor(x => new PointOfSale().Comment)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>

        <td>
            @Html.ActionLink(Business.Essentials.Resources.Edit, "Edit", new { id=item.Id }) |
            @Html.ActionLink(Business.Essentials.Resources.Details, "Details", new { id=item.Id }) |
            @Html.ActionLink(Business.Essentials.Resources.Delete, "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
