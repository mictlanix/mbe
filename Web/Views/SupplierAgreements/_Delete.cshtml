@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model SupplierAgreement

<div class='modal-header'>
    <button type='button' class='close' data-dismiss='modal'>&times;</button>
    <h3>@Resources.SupplierAgreement</h3>
</div>
<div class='modal-body'>
    @using(Ajax.BeginForm (null, null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "supplierAgreement-modal" }, new { @class = "modal-form" })) {
        <div style='overflow:hidden'>
            @Html.Hidden("OwnerId", (object) ViewBag.OwnerId)
            @Html.DisplayForModel()
        </div>
    }
</div>
<div class='modal-footer'>
    <button type='button' class='btn' data-dismiss='modal'>@Resources.Discard</button>
    <button type='submit' class='btn btn-danger' onclick='$("#supplierAgreement-modal form").submit();'>@Resources.Delete</button>
</div>


