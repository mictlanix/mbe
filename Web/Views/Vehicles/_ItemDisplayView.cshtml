@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model Vehicle

<li id='id:@Model.Id'>
    <div class='info'>
        <span class='thick'>@Html.DisplayFor(x => x.LicensePlate)</span>
        <br/>
        <b>@Resources.Amount:</b>
        <span class='editable subtotal'>@Html.DisplayFor(x => x.LicensePlate)</span>
    @if (!string.IsNullOrWhiteSpace (Model.NickName)) {
    	<br/>
        <b style='vertical-align:top;'>@Resources.Comment:</b>
        <span style='display: inline-block;'>@Html.DisplayFor(x => x.Name)</span>
    }
    </div>
</li>