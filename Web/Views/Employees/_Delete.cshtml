@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model Employee

<div class='modal-header'>
	<button type='button' class='close' data-dismiss='modal'>&times;</button>
	<h3>@Resources.Employee</h3>
</div>
<div class='modal-body'>
@using (Ajax.BeginForm(null, null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "employee-modal" }, new { @class = "modal-form" })) {
	<div style='overflow:hidden'>
		@Html.DisplayForModel()
	</div>
}
</div>
<div class='modal-footer'>
	<button type='button' class='btn' data-dismiss='modal'>@Resources.Discard</button>
	<button type='submit' class='btn btn-danger' onclick='$("#employee-modal form").submit();'>@Resources.Delete</button>
</div>