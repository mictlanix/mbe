@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model ServiceReport
<tr id='id:@Model.Id'>
    <td class="date">@Html.DisplayFor(x => x.Date)</td>
    <td>@Html.DisplayFor(x => x.Type)</td>
    <td>@Html.DisplayFor(x => x.Customer, "String")</td>
    <td class='actions'>
    	@if(ViewBag.Privilege.AllowRead){
    	    @Html.ActionLink(" ", "View", "ServiceReports", new { id = Model.Id }, new { @class = "button icon view icon-only open-modal", title = Resources.Details, data_modal_id = "service-modal" })
        }
    	@if(ViewBag.Privilege.AllowUpdate){
        	@Html.ActionLink(" ", "Edit", "ServiceReports", new { id = Model.Id }, new { @class = "button icon edit icon-only open-modal", title = Resources.Edit, data_modal_id = "service-modal" })
        }
    	@if(ViewBag.Privilege.AllowDelete){
        	@Html.ActionLink(" ", "Delete", "ServiceReports", new { id = Model.Id }, new { @class = "button icon delete icon-only open-modal", @title = Resources.Delete, data_modal_id = "service-modal" })
        }
    </td>
</tr>