@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model Address

@using (Ajax.BeginForm(new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "addressDlg" })) {
    @Html.ValidationSummary(true)
	@Html.EditorForModel()
	@Html.Hidden("owner", (object)ViewBag.OwnerId)
	<div class="action-buttons">
		<button type="button" class="cancel">@Resources.Discard</button>
		<button type="submit" class="icon save">@Resources.Create</button>
	</div>
}
<script type="text/javascript">
$('#Street').focus();
bindCloseDialog(".cancel");
</script>