@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model Notarization
<tr id='id:@Model.Id'>
    <td class="date">@Html.DisplayFor(x => x.Date)</td>
    <td>@Html.DisplayFor(x => x.NotaryOffice)</td>
    <td>@Html.DisplayFor(x => x.DocumentDescription)</td>
    <td class="date">@Html.DisplayFor(x => x.DeliveryDate)</td>
    <td class='actions'>
    	@if(ViewBag.Privilege.AllowRead){
    	    @Html.ActionLink(" ", "View", "Notarizations", new { id = Model.Id }, new { @class = "button icon view icon-only open-modal", title = Resources.Details, data_modal_id = "notarization-modal" })
        }
    	@if(ViewBag.Privilege.AllowUpdate){
        	@Html.ActionLink(" ", "Edit", "Notarizations", new { id = Model.Id }, new { @class = "button icon edit icon-only open-modal", title = Resources.Edit, data_modal_id = "notarization-modal" })
        }
    	@if(ViewBag.Privilege.AllowDelete){
        	@Html.ActionLink(" ", "Delete", "Notarizations", new { id = Model.Id }, new { @class = "button icon delete icon-only open-modal", @title = Resources.Delete, data_modal_id = "notarization-modal" })
        }
    </td>
</tr>