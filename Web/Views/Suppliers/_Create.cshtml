@using Mictlanix.BE
@model Mictlanix.BE.Model.Supplier

@*@using (Ajax.BeginForm( new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "supplier_dlg" }))
{
    @Html.ValidationSummary(true)
    @Html.EditorForModel()
}*@

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    @Html.EditorForModel()
}
