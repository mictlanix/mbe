@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model IEnumerable<FiscalDocumentRelation>

<ul id="items" class="list-items">
@foreach (var item in Model) {
    @Html.Partial("_RelationEditorView", item)
}
</ul>