@using Mictlanix.BE
@using Mictlanix.BE.Model
@using Mictlanix.BE.Web.Helpers
@model IEnumerable<FiscalDocumentRelation>

<ul id="relations-xt" class="list-items">
@foreach (var item in Model) {
    @Html.Partial("_SimpleRelationEditorView", item)
}
</ul>